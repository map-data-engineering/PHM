/.quarto/
/_site/
/.Rproj.user/
*.rds
*.docx
_quarto_internal_scss_error.scss
.Rhistory
.RData

**/*.quarto_ipynb

# Source copy of ORS key — never commit at repo root.
# The rendered copy at docs/ors-key.js IS committed so GitHub Pages can serve it
# (key is exposed to browsers either way; rotate periodically).
/ors-key.js

# WorldPop rasters (~350 MB total across 8 countries) — download via
# scripts/population/00_download_worldpop.R, never commit.
data/population/raw/

# Uploaded design template kept for reference; not part of the built site.
/_templates/
/docs/templates/
/docs/_templates/
